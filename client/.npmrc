# NPM Configuration for Vercel deployment
# Helps resolve Rollup optional dependency issues

# Use exact versions for consistency
save-exact=true

# Prefer offline packages to avoid network issues
prefer-offline=true

# Disable optional dependencies warnings
optional=false

# Engine strict for Node.js version consistency
engine-strict=true

# Legacy peer deps for compatibility
legacy-peer-deps=false