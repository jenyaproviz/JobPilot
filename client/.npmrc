# NPM Configuration for Vercel deployment
# Helps resolve Rollup optional dependency issues

# Force resolution of optional dependencies
install-strategy=hoisted
legacy-peer-deps=false

# Disable audit and funding for faster installs
audit=false
fund=false

# Force platform-specific packages
target_platform=linux
target_arch=x64
target_libc=glibc